server {
  listen 80;
  listen [::]:80;
  
  root /web;
  index index.html;
  
  location / {
    try_files $uri $uri.html /index.html =404;
  }
  location /api {
    proxy_pass http://${API_HOST}/api;
  }
}
